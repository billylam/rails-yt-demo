<p id="notice"><%= notice %></p>

<div class="row">
  <div class="span8">
    <!-- move this out of view later -->
    <% if params[:video_id] %>
      <%= render @playlist.videos.find(params[:video_id]), autoplay:1 %>
      <h2> <%= @playlist.videos.find(params[:video_id]).name %> </h2>
    <% elsif !@playlist.videos.first.nil? %>
      <%= render @playlist.videos.first %>
      <h2> <%= @playlist.videos.first.name %> </h2>
    <% end %>
    </p>

    <%= link_to 'Rename', edit_playlist_path(@playlist) %> |
    <%= link_to 'Back', playlists_path %>
    <%= render 'shared/video_form' %>
  </div>
  <div class="span4">
    <ul id="playlist">
      <%= render @playlist %>
    </ul>
  </div>
</div>
