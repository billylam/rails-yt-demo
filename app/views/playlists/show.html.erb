<p id="notice"><%= notice %></p>
<% provide :page_title, @playlist.name %>

<div class="row">
  <div class="span8">
    <% unless @playlist.videos.first.nil? %>
      <%= render @playlist.videos.first, playlist: @playlist %>
      <h2 class="videoName"> <%= @playlist.videos.first.name %> </h2>
    <% end %>
    </p>

    <%= render 'shared/video_form' %>
  </div>
  <div class="span4">
    <ul id="playlist">
      <%= render @playlist %>
    </ul>
  </div>
</div>
