<h1>Listing videos</h1>

<ul>
<% @videos.each do |video| %>
  <li>
  <%= link_to image_tag(get_thumbnail_for(video.youtube_id)), video_path(id:video.id) %>
  <%= video.name %>
  <%= video.rating %>
  </li>
<% end %>
</ul>
