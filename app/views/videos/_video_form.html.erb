<%= form_for @playlist.videos.build(playlist_id: @playlist.id) do |f| %>
  <%= f.hidden_field :playlist_id %>
  <%= f.text_field :url, placeholder: "Enter a YouTube url" %>
  <%= f.submit "Add" %>
<% end %>
