<% publish_to "/videos/add_to/#{@playlist.id}" do %>
$("#playlist").append('<%= escape_javascript(render 'playlists/playlist_item', position:@playlist.videos.length-1, video:@video) %>');
queueReloadPlaylist('<%= @video.youtube_id %>');
<% end %>
